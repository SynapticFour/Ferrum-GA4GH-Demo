# Benchmark (hap.py)

Auto-generated by `demo/run.sh` — **do not hand-edit** (regenerated on each pipeline run).

| Field | Value |
|-------|-------|
| Last run | 2026-03-22 14:32 UTC |
| Pipeline wall time (s) | 54 |
| WES engine | wdl |
| WES run id | `01KMAZBCMWCR9V3XZGW6R5PY45` |
| DRS /stream plain median (s, ref_fasta) | 0.008823291049338877 |
| DRS micro repetitions (n) | 3 |
| DRS /stream Crypt4GH **at-rest** median (s, ref_fasta, server decrypt) | 0.0022616249043494463 |
| DRS /stream client header (`X-Crypt4GH-Public-Key`) median (s) | 0.0018515419214963913 |
| Precision | 1.0 |
| Recall | 1.0 |
| F1 | 1.0 |
| Input dataset | Synthetic GIAB-style subset (22:1700-2300) |
| hap.py metrics | `results/happy.metrics.json.gz` |

## GA4GH components exercised

1. **TRS** — Dockstore `ga4gh/trs/v2` descriptor cached under `workflows/cached/`.
2. **DRS** — Files ingested via `POST /ga4gh/drs/v1/ingest/file`; the workflow engine localizes `GET .../objects/{id}/stream` (Cromwell or Nextflow; raw bytes on the compose network).
3. **WES** — `POST /ga4gh/wes/v1/runs` (WDL or Nextflow + params).
4. **TES** — Ferrum routes WES to `POST /ga4gh/tes/v1/tasks` (Docker backend: Cromwell + nested GATK, or Nextflow with Docker enabled + nested GATK).
5. **DRS micro** — `scripts/drs_micro_benchmark.py` times `GET .../objects/{id}/stream` (**n = 3** runs per mode by default). Phase 2 (`./run --macro`) compares **plaintext vs Crypt4GH-at-rest** `ref_fasta` object ids; optional PEM/base64 `X-Crypt4GH-Public-Key` for client re-encrypt experiments. See `results/drs_micro.json`.

## Publication-friendly summary

**DRS `/stream` micro-benchmark:** median wall time uses **n = 3** repeated requests per mode (plaintext storage vs **Crypt4GH-at-rest** (server decrypt on `/stream`, `--encrypted-object-id`); capped at **7.6 MiB (~8,000,000 bytes)** per request unless `--max-bytes 0`). See `scripts/drs_micro_benchmark.py` (`--repeat`, `--encrypted-object-id`).

**Dataset (local files ingested to DRS):**

| Item | Size |
|------|------|
| **BAM slice** (`na12878_slice.bam`) | 1.89 KiB (1,940 bytes) |
| **All six ingest objects** (BAM+BAI+ref+ref.fai+truth+tbi) | **6.46 KiB** (6,620 bytes) |

**Genomic interval:** `22:1700-2300` (synthetic subset).

**Cromwell vs Nextflow** (same `tiny_hc` logic, same DRS objects, same interval):

| Engine | TES executor image | Pipeline wall time (s) | WES run id (example) |
|--------|-------------------|------------------------|------------------------|
| **Cromwell** (WDL `tiny_hc.wdl`) | `broadinstitute/cromwell:…` | 54 | `01KMAZBCMWCR9V3XZGW6R5PY45` |
| **Nextflow** (`tiny_hc.nf`) | `nextflow/nextflow:24.10.3` (+ GATK container) | 25 | `01KMAVR7DCA84SRZWSRGWZ0TBS` |

Times are **end-to-end per pass** (ingest, DRS micro-bench, WES, hap.py), not GATK-only.


## Phase 2 macro (plain vs Crypt4GH at-rest ingest)

Same **WDL** workflow (`workflows/tiny_hc.wdl`), same interval, same hap.py truth — two DRS ingests on one stack: **plaintext blobs** vs **`encrypt=true`** (node key in `demo/fixtures/crypt4gh-node/`). The engine still localizes via `GET .../stream`; the gateway decrypts at rest when `is_encrypted` (see Ferrum DRS stream handler). Pipeline wall time **includes** ingest, DRS micro-bench, WES, and hap.py.

| Profile | Time (s) | WES run | Precision | Recall | F1 |
|---------|----------|---------|-----------|--------|-----|
| Plain ingest | 49 | `01KMAZ9WKAVNVE94E054SM77VZ` | 1.0 | 1.0 | 1.0 |
| Crypt4GH at-rest | 54 | `01KMAZBCMWCR9V3XZGW6R5PY45` | 1.0 | 1.0 | 1.0 |

Artefacts: `results/benchmark.phase2_plain.json`, `results/benchmark.phase2_crypt4gh.json`, `results/phase2_pass_*.json`.

