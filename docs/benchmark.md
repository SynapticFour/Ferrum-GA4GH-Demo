# Benchmark (hap.py)

Auto-generated by `demo/run.sh` — **do not hand-edit** (regenerated on each pipeline run).

| Field | Value |
|-------|-------|
| Last run | 2026-03-22 06:49 UTC |
| Pipeline wall time (s) | 55 |
| WES engine | wdl |
| WES run id | `01KMA4TCVY6D2B7DTY95FKB4QJ` |
| DRS /stream plain median (s, ref_fasta) | 0.0032104579731822014 |
| Precision | 1.0 |
| Recall | 1.0 |
| F1 | 1.0 |
| Input dataset | Synthetic GIAB-style subset (22:1700-2300) |
| hap.py metrics | `results/happy.metrics.json.gz` |

## GA4GH components exercised

1. **TRS** — Dockstore `ga4gh/trs/v2` descriptor cached under `workflows/cached/`.
2. **DRS** — Files ingested via `POST /ga4gh/drs/v1/ingest/file`; Cromwell localizes `GET .../objects/{id}/stream` (raw bytes on the compose network).
3. **WES** — `POST /ga4gh/wes/v1/runs` (WDL or Nextflow + params).
4. **TES** — Ferrum routes WES to `POST /ga4gh/tes/v1/tasks` (Docker backend: Cromwell + nested GATK, or Nextflow + `-with-docker`).
5. **DRS micro** — `scripts/drs_micro_benchmark.py` times `GET .../objects/{id}/stream` (optional `X-Crypt4GH-Public-Key`); see `results/drs_micro.json`.

## Phase 2 macro (plain vs Crypt4GH at-rest ingest)

Same WDL, same interval, same hap.py truth — two DRS ingests on one stack: **plaintext blobs** vs **`encrypt=true`** (node key in `demo/fixtures/crypt4gh-node/`). Cromwell still uses `GET .../stream`; the gateway decrypts at rest when `is_encrypted` (see Ferrum DRS stream handler). Pipeline wall time **includes** ingest, DRS micro-bench, WES, and hap.py.

| Profile | Time (s) | WES run | Precision | Recall | F1 |
|---------|----------|---------|-----------|--------|-----|
| Plain ingest | 49 | `01KMA4RWN38K5AZWQW8Y29DZRY` | 1.0 | 1.0 | 1.0 |
| Crypt4GH at-rest | 55 | `01KMA4TCVY6D2B7DTY95FKB4QJ` | 1.0 | 1.0 | 1.0 |

Artefacts: `results/benchmark.phase2_plain.json`, `results/benchmark.phase2_crypt4gh.json`, `results/phase2_pass_*.json`.

