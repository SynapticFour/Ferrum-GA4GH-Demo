# Benchmark (hap.py)

> **Claim scope: pipeline smoke.** hap.py numbers are for this slice only — not a GIAB publication result. Read `docs/CLAIMS.md` and `results/RUN_MANIFEST.json`.

Auto-generated by `demo/run.sh` — **do not hand-edit** (regenerated on each pipeline run).

| Field | Value |
|-------|-------|
| Last run | 2026-08-15 12:54 UTC |
| Pipeline wall time (s) | 58 |
| WES engine | wdl |
| WES run id | `01M02QS1S433J92KEXBJ213D9N` |
| DRS /stream plain median (s, ref_fasta) | 0.008944667000000003 |
| DRS micro repetitions (n) | 3 |
| DRS /stream Crypt4GH **at-rest** median (s, ref_fasta, server decrypt) | 0.006370166999999996 |
| Precision | 0.0 |
| Recall | 0.0 |
| F1 | 0.0 |
| Caller uses truth `--alleles` | no |
| Claim scope | pipeline_smoke |
| Input dataset | Synthetic GIAB-style subset (22:1700-2300) — pipeline smoke, not a GIAB publication benchmark |
| hap.py metrics | `results/happy.metrics.json.gz` |

*F1/precision 0 on a tiny synthetic slice with a **blind** caller is expected (GATK may emit no variants). That is not a Ferrum product failure.*

## GA4GH components exercised

1. **TRS (descriptor fetch only)** — Dockstore `ga4gh/trs/v2` JSON is cached under `workflows/cached/` and recorded in `results/trs_fetch.json` with `executed: false`. WES runs in-repo `tiny_hc.wdl` / `tiny_hc.nf`, not the Dockstore GATK germline WDL.
2. **DRS** — Files ingested via `POST /ga4gh/drs/v1/ingest/file`; the workflow engine localizes `GET .../objects/{id}/stream` (Cromwell or Nextflow; raw bytes on the compose network).
3. **WES** — `POST /ga4gh/wes/v1/runs` (WDL or Nextflow + params).
4. **TES** — Ferrum routes WES to `POST /ga4gh/tes/v1/tasks` (Docker backend: Cromwell + nested GATK, or Nextflow with Docker enabled + nested GATK). Requires the demo TES overlay; see NOTICE.
5. **DRS micro** — `scripts/drs_micro_benchmark.py` times `GET .../objects/{id}/stream` (**n = 3** runs per mode by default). Loopback/compose timing, not a WAN benchmark. **`./run --macro`:** after plain + encrypted ingests, one merged `drs_micro.json` with **`plain`** and **`crypt4gh_at_rest`** (and **`crypt4gh`** if `FERRUM_GA4GH_CRYPT4GH_PUBKEY` is set).
6. **hap.py** — concordance on **this slice only**. Caller does **not** receive `--alleles` truth. Read `results/RUN_MANIFEST.json` before citing numbers.

## Reviewer summary (pipeline smoke)

**DRS `/stream` micro-benchmark:** median wall time uses **n = 3** repeated requests per mode (plaintext storage vs **Crypt4GH-at-rest** (server decrypt on `/stream`, `--encrypted-object-id`); capped at **7.6 MiB (~8,000,000 bytes)** per request unless `--max-bytes 0`). See `scripts/drs_micro_benchmark.py` (`--repeat`, `--encrypted-object-id`).

**DRS micro JSON keys** (`results/drs_micro.json`):

| JSON key | What is measured |
|----------|------------------|
| `plain` | `ref_fasta` ingested **without** `encrypt=true`; raw bytes from storage |
| `crypt4gh_at_rest` | Second `ref_fasta` object (after `encrypt=true` ingest); **server decrypt** on `GET .../stream` |
| `crypt4gh` | Optional: same URL as `plain` with header `X-Crypt4GH-Public-Key` (PEM is normalized to **one-line base64** in the client). Stock Ferrum often does **not** re-encrypt for anonymous requests; row omitted if skipped. |

**DRS micro medians (wall time, seconds):**

| Mode | Median (s) |
|------|------------|
| Plain `ref_fasta` | 0.008944667000000003 |
| Crypt4GH **at-rest** (server decrypt) | 0.006370166999999996 |

**Encrypted `ref_fasta` object id** (at-rest leg): `01M02QS1MWX4ZXJ3R2JFT91QCB` (plain-phase mapping snapshot: `results/drs_mapping_phase_plain.json` after `./run --macro`).

**Dataset (local files ingested to DRS):**

| Item | Size |
|------|------|
| **BAM slice** (`na12878_slice.bam`) | 847 B (847 bytes) |
| **All six ingest objects** (BAM+BAI+ref+ref.fai+truth+tbi) | **5.40 KiB** (5,529 bytes) |

**Genomic interval:** `22:1700-2300` (synthetic subset).

**Cromwell vs Nextflow** (same `tiny_hc` logic, same DRS objects, same interval):

| Engine | TES executor image | Pipeline wall time (s) | WES run id (example) |
|--------|-------------------|------------------------|------------------------|
| **Cromwell** (WDL `tiny_hc.wdl`) | `broadinstitute/cromwell:…` | 58 | `01M02QS1S433J92KEXBJ213D9N` |
| **Nextflow** (`tiny_hc.nf`) | `nextflow/nextflow:24.10.3` (+ GATK container) | 69 | `01M024H2ZBHV557FJ34CRH8CE4` |

Times are **end-to-end per pass** (ingest, DRS micro-bench, WES, hap.py), not GATK-only.


## Phase 2 macro (plain vs Crypt4GH at-rest ingest)

Same **WDL** workflow (`workflows/tiny_hc.wdl`), same interval, same hap.py truth — two DRS ingests on one stack: **plaintext blobs** vs **`encrypt=true`** (node key in `demo/fixtures/crypt4gh-node/`). The engine still localizes via `GET .../stream`; the gateway decrypts at rest when `is_encrypted` (see Ferrum DRS stream handler). Pipeline wall time **includes** ingest, DRS micro-bench, WES, and hap.py.

| Profile | Time (s) | WES run | Precision | Recall | F1 |
|---------|----------|---------|-----------|--------|-----|
| Plain ingest | 59 | `01M02QQ74Z1CYMND5226R4GZ1A` | 0.0 | 0.0 | 0.0 |
| Crypt4GH at-rest | 58 | `01M02QS1S433J92KEXBJ213D9N` | 0.0 | 0.0 | 0.0 |

Artefacts: `results/benchmark.phase2_plain.json`, `results/benchmark.phase2_crypt4gh.json`, `results/phase2_pass_*.json`, `results/drs_mapping_phase_plain.json` (plain-phase DRS ids), **`results/drs_micro.json`** (merged **after** both passes: `plain` + `crypt4gh_at_rest` + optional `crypt4gh`).


## Africa Resilience Features

<!-- AFRICA_FEATURES_START -->
| Feature | Status |
|---------|--------|
| Offline Mode (SQLite) | not detected |
| ONT/Nanopore Ingestion | detected (probe only — see africa_results.json) |
| Multi-Pathogen Beacon | detected (probe only — see africa_results.json) |
| Outbreak Mode | detected (probe only — see africa_results.json) |
| Data Residency Audit | detected (probe only — see africa_results.json) |
| Reference Registry | detected (probe only — see africa_results.json) |
<!-- AFRICA_FEATURES_END -->

*Probes are not a pass. `results/africa_results.json` `summary.verdict` is the contract (`passed` / `failed` / `not_evaluated`). Skip-only is not a pass.*
